import 'package:flutter/material.dart';

class Cameramodel extends StatelessWidget {
  const Cameramodel({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
